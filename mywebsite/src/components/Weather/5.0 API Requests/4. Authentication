4 tiers:
0. no authentication
    rate limit (limits the number of uses in a particular period of time)
1. basic authentication
    username/password
    Base64 - text taking ASCII that can be converted into bits, then convert the bits into another character
        basically layered coding using ASCII, so A means squiggly, or a series of numbers.
        You an add the base64 piece into your authorization header
2. API key authorization
3. Token Based authentication